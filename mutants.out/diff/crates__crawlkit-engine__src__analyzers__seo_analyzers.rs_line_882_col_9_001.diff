--- crates/crawlkit-engine/src/analyzers/seo_analyzers.rs
+++ replace <impl Analyzer for WordCountAnalyzer>::name -> &str with "xyzzy"
@@ -874,17 +874,17 @@
 impl Default for WordCountAnalyzer {
     fn default() -> Self {
         Self::new()
     }
 }
 
 impl Analyzer for WordCountAnalyzer {
     fn name(&self) -> &str {
-        "word-count"
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 
     fn analyze(&self, ctx: &AnalysisContext) -> Vec<Finding> {
         let mut findings = Vec::new();
         let url = &ctx.page.url;
 
         // Both stats come from the parser's single visible-text walk, so
         // the average is over a consistent corpus. (The previous heading-
