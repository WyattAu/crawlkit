[package]
name = "crawlkit-budget-check"
description = "CI budget gate: committed P50/P99 percentile budgets vs criterion output (estate standard §5), via percentile-kit"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
# CI tooling, not a published product crate.
publish = false

[lints]
workspace = true

[dependencies]
percentile-kit = { version = "0.1", default-features = false, features = ["report"] }
thiserror = { workspace = true }
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
tempfile = "3"
toml = { workspace = true }
