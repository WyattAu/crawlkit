--- crates/crawlkit-engine/src/analyzers/seo_analyzers.rs
+++ replace <impl Analyzer for MetaTagAnalyzer>::name -> &str with "xyzzy"
@@ -430,17 +430,17 @@
 impl Default for MetaTagAnalyzer {
     fn default() -> Self {
         Self::new()
     }
 }
 
 impl Analyzer for MetaTagAnalyzer {
     fn name(&self) -> &str {
-        "meta-tags"
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 
     fn analyze(&self, ctx: &AnalysisContext) -> Vec<Finding> {
         let mut findings = Vec::new();
         let url = &ctx.page.url;
         let meta = &ctx.page.meta;
 
         // --- Title ---
