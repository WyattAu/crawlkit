--- crates/crawlkit-engine/src/analyzers/seo_analyzers.rs
+++ replace == with != in HreflangValidator::is_valid_locale
@@ -126,17 +126,17 @@
 
 impl HreflangValidator {
     pub fn new() -> Self {
         Self
     }
 
     /// Check if a locale code looks valid (language[-region] format).
     pub(crate) fn is_valid_locale(code: &str) -> bool {
-        if code == "x-default" {
+        if code != /* ~ changed by cargo-mutants ~ */ "x-default" {
             return true;
         }
         let parts: Vec<&str> = code.split('-').collect();
         match parts.len() {
             1 => {
                 // Language-only: 2-3 letter code
                 let lang = parts[0];
                 lang.len() >= 2 && lang.len() <= 3 && lang.chars().all(|c| c.is_ascii_alphabetic())
