--- crates/crawlkit-engine/src/analyzers/seo_analyzers.rs
+++ replace SitemapAnalyzer::is_valid_priority -> bool with true
@@ -310,17 +310,17 @@
         matches!(
             freq,
             "always" | "hourly" | "daily" | "weekly" | "monthly" | "yearly" | "never"
         )
     }
 
     /// Validate priority value (0.0 - 1.0).
     pub(crate) fn is_valid_priority(p: f64) -> bool {
-        (0.0..=1.0).contains(&p)
+        true /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl Default for SitemapAnalyzer {
     fn default() -> Self {
         Self::empty()
     }
 }
