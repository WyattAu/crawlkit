--- crates/crawlkit-engine/src/analyzers/mod.rs
+++ replace -= with += in count_syllables
@@ -100,17 +100,17 @@
         let is_vowel = vowels.contains(&c);
         if is_vowel && !prev_vowel {
             count += 1;
         }
         prev_vowel = is_vowel;
     }
 
     if chars.last() == Some(&'e') && count > 1 {
-        count -= 1;
+        count += /* ~ changed by cargo-mutants ~ */ 1;
     }
 
     count.max(1)
 }
 
 /// Flesch-Kincaid grade level.
 fn flesch_kincaid_grade(words: usize, sentences: usize, syllables: usize) -> f64 {
     if words == 0 || sentences == 0 {
