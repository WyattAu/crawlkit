--- crates/crawlkit-engine/src/analyzers/seo_analyzers.rs
+++ replace <impl Analyzer for SitemapAnalyzer>::name -> &str with ""
@@ -322,17 +322,17 @@
 impl Default for SitemapAnalyzer {
     fn default() -> Self {
         Self::empty()
     }
 }
 
 impl Analyzer for SitemapAnalyzer {
     fn name(&self) -> &str {
-        "sitemap"
+        "" /* ~ changed by cargo-mutants ~ */
     }
 
     fn analyze(&self, ctx: &AnalysisContext) -> Vec<Finding> {
         let mut findings = Vec::new();
         let url = &ctx.page.url;
 
         if self.known_urls.is_empty() {
             // No sitemap data was loaded for this crawl; sitemap validation
